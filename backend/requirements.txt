# MuxMinus Backend Requirements
# Audio separation service using Demucs

# FastAPI framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# PyTorch - For CUDA support, install with:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
# For CPU-only:
#   pip install torch torchaudio
torch>=2.1.0
torchaudio>=2.1.0
torchcodec>=0.9.1

# Demucs audio separation
demucs>=4.0.1
soundfile>=0.12.0  # Required by torchaudio for WAV file support

# Whisper speech-to-text transcription
openai-whisper>=20231117

# Audio metadata extraction
mutagen>=1.47.0

# Additional dependencies
python-multipart>=0.0.6  # For file uploads if needed